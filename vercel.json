{
  "buildCommand": "cd pmcs-app && npm install && npm run build && cp build/index.html build/app.html && cp build/landing.html build/index.html",
  "outputDirectory": "pmcs-app/build",
  "framework": null,
  "rewrites": [
    { "source": "/app", "destination": "/app.html" }
  ]
}
